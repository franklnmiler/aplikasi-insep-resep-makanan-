<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/gambar67"
    android:padding="16dp"
    android:gravity="center_horizontal">


    <TextView
        android:id="@+id/deviceInfo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:paddingBottom="8dp"
        android:text="Device Info"
        android:textColor="@android:color/black"
        android:textSize="19sp"
        android:visibility="gone" />

    ```
    <!-- Spacer untuk memberikan ruang di atas -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical" />

    <!-- Teks mirip seperti di gambar -->
    <TextView
        android:id="@+id/aestheticText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:text="Racik Makanan Sesuka Hati Di sini"
        android:textColor="#000000"
        android:textSize="18sp"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:textStyle="bold"
        android:shadowColor="#66000000"
        android:shadowDx="2"
        android:shadowDy="44"
        android:shadowRadius="44" />

    <!-- Tombol transparan sebagian -->
    <Button
        android:id="@+id/btnSaveDevice"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Get Started"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:elevation="8dp"
        android:fontFamily="sans-serif-medium"
        android:layout_marginBottom="32dp"
        android:backgroundTint="#CCF84137"
        android:background="@drawable/button_register_background" />
    ```

</LinearLayout>
